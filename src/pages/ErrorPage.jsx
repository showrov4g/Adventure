import React from 'react';

const ErrorPage = () => {
    useEffect(()=>{
        document.title= "Eco | ErrorPage"
      },[])
    return (
        <div>
            nothing found
        </div>
    );
};

export default ErrorPage;